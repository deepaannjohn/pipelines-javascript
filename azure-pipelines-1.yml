

trigger:
- master

stages:
  - stage: build
    condition: eq(variables.shouldRun, 'build')
    displayName: 'Build'
    jobs:
    - job: build
      
      steps:
        - task: NodeTool@0
          inputs:
            versionSpec: '10.x'
          displayName: 'Install Node.js'

        - script: |
            npm install
            npm run build
          displayName: 'npm install and build'


        - task: CopyFiles@2
          inputs:
            targetFolder: $(Build.ArtifactStagingDirectory)
        - task: PublishBuildArtifacts@1
          inputs:
            pathToPublish: $(Build.ArtifactStagingDirectory)
            artifactName: MyBuildOutputs
    
  - stage: test
    condition: eq(variables.shouldRun, 'test')
    displayName: 'Test'
    jobs:
      - job: test
        steps:
          - script: 
              echo 'All test cases passes'
            displayName: 'Unit test'
      

    

  - stage: deploy

  
    
    condition: eq(variables.shouldRun, 'deploy')
    displayName: 'Deploy'
    jobs:
      - job: 
        steps:
          - script: echo ${{ variables.pipelineid1 }} 
          - script: echo $(pipelineid1) 
          - script: echo $(variables.pipelineid1) 
          - task: DownloadPipelineArtifact@2
            inputs:
              buildType: 'specific'
              project: 'dfc7d9bc-d749-475a-ae1b-113601aff5ce'
              definition: '20'
              buildVersionToDownload: 'specific'
              pipelineId: $[variables.pipelineid1]
              artifactName: 'MyBuildOutputs'
              targetPath: '$(Pipeline.Workspace)'
      
          - script: echo ls $(Pipeline.Workspace)
          - script: ls -ltr
          - script: ls


                  
      - deployment: deployToDev
        displayName: 'Deploy Dev'
        
        environment: Dev
        strategy:
          runOnce:
            deploy:


              
              steps:
                

                - script: echo Deployed to Dev
                - script: ls 

